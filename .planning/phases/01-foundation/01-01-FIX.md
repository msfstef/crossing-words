---
phase: 01-foundation
plan: 01-FIX
type: fix
---

<objective>
Fix 3 UAT issues from plan 01-01.

Source: 01-01-ISSUES.md
Priority: 0 critical, 1 major, 2 minor
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

**Issues being fixed:**
@.planning/phases/01-foundation/01-01-ISSUES.md

**Original plan for reference:**
@.planning/phases/01-foundation/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Fix UAT-001: Update browser tab title</name>
  <files>index.html</files>
  <action>
Update the `<title>` tag in index.html from "temp-vite" to "Crossing Words".

This is a straightforward HTML change in the head section.
  </action>
  <verify>Open app in browser, tab title shows "Crossing Words"</verify>
  <done>Browser tab displays "Crossing Words"</done>
</task>

<task type="auto">
  <name>Fix UAT-002: Add collaborative crossword subtitle</name>
  <files>src/App.tsx</files>
  <action>
Add a subtitle below the "Crossing Words" heading that explains it's a collaborative crossword app.

Add text like "Collaborative crossword puzzles" or similar descriptive subtitle between the heading and the "coming soon..." text.

Style it appropriately (smaller than heading, same light text color).
  </action>
  <verify>Open app in browser, subtitle text visible explaining collaborative crossword</verify>
  <done>App shows descriptive subtitle about collaborative crosswords</done>
</task>

<task type="auto">
  <name>Fix UAT-003: Fix PWA icons</name>
  <files>public/icon-192.svg, public/icon-512.svg, vite.config.ts</files>
  <action>
Investigate and fix the broken PWA icons. Common causes:
1. SVG syntax errors in the icon files
2. Wrong MIME type or path in manifest configuration
3. Icons not being served correctly

Check:
1. Validate SVG syntax in icon-192.svg and icon-512.svg
2. Verify vite-plugin-pwa manifest.icons configuration has correct paths and types
3. Ensure icons use `type: 'image/svg+xml'` for SVG format

If SVGs are malformed, fix them or recreate with valid SVG markup.
If manifest config is wrong, fix the icons array in vite.config.ts VitePWA plugin options.
  </action>
  <verify>Run `npm run build && npm run preview`, check DevTools > Application > Manifest, icons display correctly</verify>
  <done>PWA icons load and display in manifest viewer</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Browser tab shows "Crossing Words"
- [ ] App displays collaborative crossword subtitle
- [ ] PWA icons display correctly in DevTools manifest viewer
- [ ] `npm run build` still succeeds
- [ ] No console errors
</verification>

<success_criteria>
- All 3 UAT issues from 01-01-ISSUES.md addressed
- Build passes
- Ready for re-verification with /gsd:verify-work 01-01
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-FIX-SUMMARY.md`
</output>
