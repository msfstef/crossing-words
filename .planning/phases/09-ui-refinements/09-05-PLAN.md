---
phase: 09-ui-refinements
plan: 05
type: execute
---

<objective>
Add puzzle title display above the grid (in addition to header) and perform final visual polish.

Purpose: Show puzzle title prominently above the grid for context, and ensure all UI elements are polished.
Output: Puzzle title displayed above grid and attribution, final visual consistency check.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-refinements/09-CONTEXT.md

**Relevant source files:**
@src/App.tsx
@src/App.css

**Prior plans:**
@.planning/phases/09-ui-refinements/09-01-SUMMARY.md (if exists)
@.planning/phases/09-ui-refinements/09-02-SUMMARY.md (if exists)
@.planning/phases/09-ui-refinements/09-03-SUMMARY.md (if exists)
@.planning/phases/09-ui-refinements/09-04-SUMMARY.md (if exists)

**Issues from CONTEXT.md:**
- Display puzzle title above the grid, above the attribution
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add puzzle title above grid</name>
  <files>src/App.tsx, src/App.css</files>
  <action>
Add puzzle title display above the grid in the solve view:
1. In gridContent (App.tsx), add puzzle title above the author attribution
2. Display puzzle.title as a heading (h1 or h2 depending on visual hierarchy)
3. Position: centered above grid, above "by {author}" text
4. Style: larger font than author, centered, readable

Order in grid area should be:
1. Connecting banner (if connecting)
2. Error banner (if error)
3. Puzzle title (new)
4. Puzzle author attribution (existing)
5. CrosswordGrid

CSS styling:
- .puzzle-title: centered, prominent font size (1.25rem or similar)
- Match existing color scheme (--color-text)
- Margin below to separate from author/grid

Note: This is in addition to the compact title in the header. The grid area title provides clear puzzle identification while solving.
  </action>
  <verify>Puzzle title displays above grid and author attribution</verify>
  <done>Puzzle title shown prominently above grid, proper visual hierarchy</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Puzzle title above grid and complete Phase 9 UI refinements</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Open any puzzle from library
    3. Verify puzzle title is displayed above the grid (in addition to header)
    4. Verify visual hierarchy: Title > Author > Grid

    **Full Phase 9 verification:**
    5. Library view at 390px:
       - FAB in bottom-right, expands to Import/Download
       - Puzzle cards show circular progress (not bars)
       - Download creates ghost entry, doesn't auto-open
    6. Puzzle view:
       - Settings gear opens menu with Check/Reveal/Auto-check/Theme sections
       - Share button is neutrally styled, next to Settings
       - Clue bar shows wrapped text, tapping toggles direction
       - First clue auto-selected on open
       - Puzzle title shown above grid
    7. Test complete flow: Download puzzle → see ghost → puzzle appears → tap to open → verify all puzzle view features
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Puzzle title displays above grid
- [ ] Visual hierarchy is clear (title > author > grid)
- [ ] Title is readable and centered
- [ ] npm run build succeeds
- [ ] All Phase 9 features working together
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Puzzle title displayed above grid
- Phase 9 complete - all UI refinements shipped
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-refinements/09-05-SUMMARY.md`

This summary should include Phase 9 completion status:
- FAB with Import/Download options
- Optimistic download UI with ghost entries
- Circular progress indicators
- Consolidated Settings menu with Check/Reveal/Auto-check
- Relocated Share button with neutral styling
- Improved clue bar with text wrapping and tap-to-toggle
- Auto-select first clue on puzzle open
- Puzzle title above grid
</output>
