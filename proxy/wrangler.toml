name = "crossing-words-proxy"
main = "src/index.ts"
compatibility_date = "2024-01-01"
account_id = "fed2683481f4e710185cbeaac94262e2"

[vars]
ALLOWED_ORIGINS = "*"

# Durable Objects for WebSocket signaling
[[durable_objects.bindings]]
name = "SIGNALING_ROOM"
class_name = "SignalingRoom"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["SignalingRoom"]

# v2 was WebsocketSync, now deleted
[[migrations]]
tag = "v2"
new_sqlite_classes = ["WebsocketSync"]

# v3: Delete WebsocketSync (no longer needed)
[[migrations]]
tag = "v3"
deleted_classes = ["WebsocketSync"]
